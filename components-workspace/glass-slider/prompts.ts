import type { Platform } from '../../app/components/ComponentCard'

export const prompts: Record<Platform, string> = {
  V0: 'Prompt pending — component under visual review.',
  Bolt: 'Prompt pending — component under visual review.',
  Lovable: 'Prompt pending — component under visual review.',
  'Claude Code': 'Prompt pending — component under visual review.',
  Cursor: 'Prompt pending — component under visual review.',
}
