import type { Platform } from '../../app/components/ComponentCard'

// Prompts are written after the visual is approved (pipeline step 5).
export const prompts: Partial<Record<Platform, string>> = {}
