# Component Status

> **2026-06-09 reconciliation.** Audited every row against git + the live registry. All `published` rows below were verified returning HTTP 200 at `aicanvas.me/r/<slug>.json`. Rows previously marked `awaiting-commit` / `committed` / `integrated` that are in fact live were promoted to `published`. Note: a component's folder name may differ from its public slug (folder `avatar-picker` ships as `meet-the-crew`; `floating-cards` ships as `traveldeck`) — rows are keyed by the public slug.

| Component | Stage | Notes |
|---|---|---|
| crypto-swap | committed (PARKED) | Local commit `ce494ad` on `ideation` 2026-06-14. **NOT pushed / NOT merged to main** — user wants Andromeda etc. shipped to production first; HOLD push/merge until explicit instruction. Uniswap-style crypto→crypto swap; ETH→BTC default; live conversion with price impact + network fee + slippage; real CC0 coin icons (inlined, no dep); contained listbox token picker (keyboard a11y, flip-up, trigger-tracking); near-black/periwinkle borderless skin; adaptive decimals; reduced-motion throughout. Passed an adversarial 18-bug audit + Reviewer PASS. Screenshot `ik.imagekit.io/aitoolkit/crypto-swap.png?v=2`, JSON/CLI check PASS. |
| mood-tracker | committed (PARKED) | Local commit `da7bb8a` on `ideation` 2026-06-14. **NOT pushed / NOT merged to main** — parked alongside crypto-swap; user is collecting more before any production ship; HOLD until explicit instruction. Reframed from the abandoned `reputation-card` (which is deleted): a "How are you feeling?" mood check-in. Six hand-drawn SVG mood characters (Frustrated/Surprised/Happy/Uneasy/Sad/Anxious), segmented mood slider (drag + keyboard) + tappable face legend, soft mood-tinted liquid-gradient panel + whole-card mood wash, Save→"Recorded" button. dualTheme via card-aware useTheme (#009), reduced-motion, 320px, framer-motion only (no icon dep). Reviewer PASS, prompts.ts (3 platforms), spec synced, component-copy added. Screenshot `ik.imagekit.io/aitoolkit/mood-tracker.png?v=1` (Happy card), JSON/CLI check PASS. |
| interactive-card-stack | awaiting-commit | Renamed from `scatter-pile` 2026-06-09 (never published, so slug/folder/function/ImageKit folder all rebased to the SEO name "Interactive Card Stack"). Loose pile of 5 polaroid-style photo cards, mixed aspect ratios (2 landscape + 3 portrait), slot-based order array (slot 0 = focused front; click/drag/arrow keys cycle), per-card idle breathing, focused card breathes 2x, mobile tightens scatter but keeps all 5 visible. Content swapped to 5 birds (scarlet macaw, toco toucan, blue-and-gold macaw, green-headed tanager, northern mockingbird), compressed + SEO-named JPEGs hosted at ik.imagekit.io/aitoolkit/interactive-card-stack/. prompts.ts written (CC/Lovable/V0), spec synced, card thumbnail at ik.imagekit.io/aitoolkit/interactive-card-stack.png?v=1, JSON check PASS. Awaiting user verify → commit. |
| dynamic-island-calculator | deleted | Removed by user request 2026-05-10. Was at `built` stage — never committed, never pushed, no public registry URL. User decided to pause web replicas of native iOS UI in favour of designing a separate iOS/SwiftUI tier on AI Canvas. Folder, registry import + entry, generated code/JSON, and registry lint output all cleaned. |
| new-project-modal | published | Commit 82c41ca on `main` (cherry-picked from ideation 47bc551, bundled with voice-chat-pill). Live at aicanvas.me/r/new-project-modal.json. Morph animation, 7-swatch color picker, always-light modal, title validation, checkmark Create. Screenshot at ik.imagekit.io/aitoolkit/new-projectmodal.png. |
| voice-chat-pill | published | Commit 82c41ca on `main` (cherry-picked from ideation 47bc551, bundled with new-project-modal). Live at aicanvas.me/r/voice-chat-pill.json. Live-session presence pill (animated speaking indicator + overlapping avatars) opens a participants modal. Source `<img>` uses `size-full` not `h-full` to avoid the generator's h-full→min-h-screen transform breaking avatar imgs. Screenshot at ik.imagekit.io/aitoolkit/voice-chat-pill.png?v=2026050501. |
| halo-type | published | Live: aicanvas.me/r/halo-type.json returns 200 (verified 2026-06-09). Commit 5181494 (bundled with stack-tower), on origin/main. Detail: Kinetic typography — 3D ring of text ("COPY ✦ PASTE ✦ SHIP ✦ REPEAT ✦") rotating around Y axis. Final tuning: tilt 24° rest=hover (no change on engage), 14s/turn, 0.3× slow on hover, STAR_SCALE 0.65. Pure CSS 3D + Framer Motion (`useAnimationFrame` + MotionValue, no Three.js). Back-arc reads upside-down (not mirrored) via twin glyph `rotateY(180)+rotateZ(180)`; per-letter opacity = cos(effective_angle) via refs (no backface-visibility — avoids pixel-shred at ±90° seam). `transformOrigin: 50% 50%` (critical — `0 0` displaces narrow letters). DOM-based per-character measurement via hidden inline-block spans + ResizeObserver (handles late Manrope load). Proportional angular spacing. Horizontal edge mask (18%/82% falloff) hides residual seam. dualTheme (dark #0A0A0A/#F5F1E8/#7A756C, light inverted). Reducedmotion freezes at 30°. Unified pointer handlers (mouse+touch). Integrator finalized: real prompts wired, ImageKit screenshot live at `halo-type.png`, description refined (88 chars). Install comment corrected `framer-motion react`. JSON check PASS. (Reconciled 2026-06-09: prior "not committed, not pushed" note was stale.) |
| slice-type | published | Live: aicanvas.me/r/slice-type.json returns 200 (verified 2026-06-09). LIGHT/NIGHT dual-lexicon illusion. L absolute at container-left (aligns with N's vertical stroke); IGHT absolute at right:0; real layout gap between them at rest. L_REST_NUDGE_PX=-1.5 compensates font side-bearing. On hover: top clip opens + lifts (-65%), bottom clip opens + sinks (+65%), bg #0A0A0A→#EFEEE6 + fg #EFEEE6→#0A0A0A via same engageSmooth MotionValue. Spring 140/18/0.9. Intro teaser on mount (700ms delay, 0.7 peak). No useTheme — always NIGHT at rest. prompts.ts written, screenshot uploaded (slice-type.png?v=1, hybrid on dark bg), JSON check PASS. (Reconciled 2026-06-09: prior "awaiting commit" note was stale.) |
| stack-tower | published | Live: aicanvas.me/r/stack-tower.json returns 200 (verified 2026-06-09). Commit 5181494 (bundled with halo-type), on origin/main. Detail: Kinetic typography — 12 stacked rows alternating "STACK"/"TOWER"; per-row transform (translateX + skewX + scale) from cos/sin(phase + rowOffset), rowOffset = i*0.35 rad so rotation travels down the stack. Final tuning: SECONDS_PER_CYCLE=5, AMPLITUDE_PX=22, all rows advance at same rate — hover does NOT modulate speed. Hover reaction: hovered row eases color toward orange accent #F16D14 and scale bumps +10%; rest unchanged. HOVER_EASE_RATE=10/s, frame-rate-independent (1-exp(-rate·dt)). Hovered index stored in ref (not state) so rotation stays smooth. One central useAnimationFrame in parent drives per-row MotionValues via `useMemo(...motionValue(0))`. Each row wrapped in pointer-target div so hover hits stay rectangular regardless of inner scaleX. Top+bottom 22% gradient fades for infinite-column feel. dualTheme (#0A0A0A/#EFEEE6 swap, dim #3A3936/#C7C3B8). font-size clamp(1.75rem,9vw,4.5rem). Reduced-motion freezes each row at phase = 0.2 + i*0.03. Integrator-done: prompts.ts written (Claude Code/Lovable/V0), spec.md synced, description refined, screenshot with orange-highlighted centre row uploaded to ImageKit (`stack-tower.png?v=1`), `// npm install framer-motion react`, JSON check PASS. (Reconciled 2026-06-09: prior "awaiting commit/push" note was stale.) |
| radial-cards | published | Live: aicanvas.me/r/radial-cards.json returns 200 (verified 2026-06-09; user confirmed ready). 7-card radial sunburst (Steps/Calories/Sleep/Water/Heart/Active/Distance — Focus dropped, going from 8 → 7). Layout: all 7 cards share a single anchor at canvas centre via `transform-origin: 0% 100%` (bottom-LEFT pivot), each rotated by hand-tuned `SLOT_ANGLES` from user's Figma file (node 318:327 — `[-35.55, 22.14, 71.01, 122.25, 161.58, -150.4, -108.41]`). Card visual: 220×150, pastel theme top zone (PASTELS array of 7 hexes from Figma) + inner white-87% panel (16px uniform radius), uniform 20px outer radius. Header: Phosphor icon (Footprints/Fire/Moon/Drop/Heart/Lightning/Path, weight=regular, size 14, theme-tinted) + dark-gray title 14px Bold. Body: dark-gray label 10px (0.4px tracking), metric 28px in DARKS[i], pill solid DARKS[i] with white 11px text. Drop shadow: `-8px 16px 32px rgba(0,0,0,0.12), -3px 6px 14px rgba(0,0,0,0.18)`. **NO flip, no back face, no sparkline** — front face only. Engagement: click/tap toggles (no hover). Engaged card glides to canvas centre with `centeringFactor` interpolating translate from `(0,-100%)` to `(-50%,-50%)`, lifts to `translateZ(120px)`, scales to 1.18, derotates to 0. **Shortest-path rotation**: per-card `cardRotation` MotionValue with two modes — FOLLOW (tracks idleRotation+slot continuously, no spring lag) and SPRING (during transitions). On engagement state change, computes `shortestEquivalent(current, target)` so spring traverses ≤180° in any single transition. **Persistent z-order**: `topIndex` state tracks last-clicked card; reactive zIndex priority — `lift>4`→50, engaged→40, isTop→30, default→1. So just-clicked card stays on top of untouched neighbours after returning to slot, until another card is clicked. Idle: bouquet rotates 45s/turn (180s reduced-motion) via single shared MotionValue mod-bounded to [0,360) in `useAnimationFrame` callback; speedSmooth eases to 0 when engaged. Springs: `LIFT_SPRING={140,28,1}` for lift/scale/centering/cardRotation; `SPRING={200,24,1}` for redistribution. 7→6 redistribution at 60° spacing when something engaged. Stage `clamp(360,60vw,700px)`. Pure CSS 3D + Framer Motion + Phosphor Icons. Reconciled 2026-06-09: the prior "NOT READY / never committed / stub entry / prompts.ts not written / no screenshot" notes were ALL stale. Component is now committed, on origin/main, with a full prompts.ts (12KB) + spec.md, a real registry entry, generated JSON, and a live screenshot. Live at aicanvas.me/r/radial-cards.json (200). |
| ripple-type | published | Live: aicanvas.me/r/ripple-type.json returns 200 (verified 2026-06-09). Kinetic typography — single word "RIPPLE" SVG text with feTurbulence (numOctaves 1, seed 4) + feDisplacementMap filter. Paired with a small 3-blade fan icon (left of word, click-toggle, OFF/ON label red→green). MAX_SCALE 36, OSCILLATION_HZ 0.09, REST_FREQ 0.001 / HOVER_FREQ 0.009, RIPPLE_PHASE_RATE 0.9, LEVEL_COUNT 1 (1s ramp / 2.5s decay). Skew up to 13° at peak. SVG viewBox 500×180, text x=250 textAnchor=middle, font-size 140, weight 900, letter-spacing -0.04em. Unique filter id via useId. dualTheme (#0A0A0A/#EFEEE6 swap), radial vignette. Reduced-motion clamps peak amplitude to 14, skew to 0, slower blade rate. Single-row fluid mobile: fan `clamp(56,11vw,112)`, gap `clamp(14,3vw,32)`, text width `min(55vw,500)` — works at 320px. Integrator-done: rippleTypePrompts wired in registry (3 platforms, constants inlined), 'ripple-type' removed from SKIP_FOLDERS in scripts/generate-registry.mjs, screenshot interaction added to scripts/screenshot.mjs (clicks fan + 1400ms wait for peak), ImageKit live at `ripple-type.png?v=1`, description refined (85 chars). JSON check PASS. (Reconciled 2026-06-09: prior "awaiting commit/push" note was stale.) |
| wild-morph | published | Live: aicanvas.me/r/wild-morph.json returns 200 (verified 2026-06-09). Italic SVG word "wild" on warm off-white panel, cursor-Y selects top/bottom corner-pin warp via 2D homography solved per-frame (matrix3d), underdamped spring physics (stiffness 280, damping 14/16, mass 1.8). Anton font, hardcoded dark (dualTheme: false). Registry entry appended, `// npm install` + `// font: Anton` comments present, screenshot uploaded to ImageKit (active upper-half warp state captured), JSON check PASS, byte-for-byte parity verified via local `shadcn add` into a temp dir. (Reconciled 2026-06-09: prior "not committed" note was stale.) |
| responsive-letters | published | Interactive text — cursor proximity animates weight (100→900), width (100%→200%), letter spacing (0em→0.4em), skew (±18°), italic toggle. Science Gothic font, neon cyan on dark navy (light/dark mode swapped). 300px influence radius, elastic spring animations, 5rem-9rem responsive typography. |
| murmuration | deleted | Removed by user request |
| sticker-wall | published | Feedback Wall — matter.js physics, 10 quote + 12 emoji seeds, pastel stickers with 32px radius, periwinkle Send key, dual-theme, title + subtitle, settled-pile screenshot. Promoted from /preview/idea-two. |
| easter-rabbit | deleted | Removed by user request |
| traveldeck | published | Built before agent system — slug renamed from floating-cards (folder unchanged) |
| text-blur-reveal | published | Built before agent system |
| particle-sphere | published | Built before agent system |
| polaroid-stack | published | Built before agent system |
| text-layout | deleted | Folder `components-workspace/text-layout/` is gone, no registry entry, and aicanvas.me/r/text-layout.json 404s. Was stale-labelled `integrated`; corrected to `deleted` during 2026-06-09 reconciliation. (Flag: confirm this removal was intentional — it was a pre-agent-system component.) |
| radial-toolbar | published | Radial context menu wheel, formatting tools, dualTheme, mobile-ready |
| glitch-button | published | Terminal-style button with hover glitch text effect |
| flip-calendar | published | Desk calendar 1–31, swipe-only, sand page stack, subtle scan line |
| charging-widget | published | Circular charging indicator, liquid waves, count-up loop — dualTheme |
| runway-loader | published | Airplane taxis down progress bar, takes off at 100% |
| dot-grid | published | Canvas dot grid, hover illuminates nearby dots — dualTheme |
| blind-pull-toggle | published | Window-blind pull cord toggle, venetian slat icon swap, cord spring animation — dualTheme |
| pill-toggle | published | iOS-style pill slider, grey→green track, spring thumb, responsive — dualTheme |
| mark-toggle | published | Earth/sand pill toggle, X→checkmark icon morph on thumb, spring animation — dualTheme |
| taga-toggle | published | Playful pill toggle, dead face (×× eyes) → happy face (arc eyes, smile), grey→yellow — dualTheme |
| x-grid | published | v2 live: breathing diagonal wave (±30%), RADIUS 340, ~1s trail decay, connection mesh, dualTheme, V0 prompt lane added |
| noise-bg | published | Random grain dots, Gaussian glow, cached neighbour connections on hover, dualTheme |
| circle-field | deleted | Removed by user request |
| bubble-field | published | Grid circles burst+reform on hover, cyclic phase animation, pastel blue palette, dualTheme |
| wave-lines | integrated | **BROKEN / not live — needs a decision.** Wired into `app/lib/component-registry.tsx` (slug `wave-lines`; folder `components-workspace/wave-lines/` has index.tsx + prompts.ts + spec.md), but `generate-registry.mjs` emits NO `wave-lines.json`, it's absent from the registry.json index, and aicanvas.me/r/wave-lines.json 404s. The homepage entry exists but the component is not installable. Either fix generation or remove the dead entry. Visual: vertical sine-wave lines, layered waves + Y-drift, extreme hover waves, dualTheme — renamed from silk-lines. Found during 2026-06-09 reconciliation. |
| distortion-grid | published | Slow sweeping sine waves, cursor repels grid outward, dualTheme — renamed from wave-grid, line alpha bumped to 0.55/0.75 |
| grid-lines | published | 20px dot grid with H/V lines, viewport-relative Gaussian halo (30% of max dim) + radial wave pulse on hover, dualTheme |
| origami-card | deleted | Removed by user request |
| kinetic-vortex | deleted | Removed by user request |
| glass-sidebar | published | Two-state glassmorphism sidebar, squircle icons, spring width, staggered labels, pink flower bg |
| glass-weather-widget | deleted | Removed by user request |
| glass-search-bar | published | Frosted glass search bar, orange active glow, suggestion dropdown with clear buttons, spring animations |
| glass-input | deleted | Removed by user request |
| glass-toast | published | Glass toast notifications, 4 variants, auto-dismiss progress bar, spring stacking |
| glass-stepper | published | Glass numeric stepper, spring number flip, notification-style tinted buttons, configurable min/max/step |
| glass-progress | published | Glass progress bar panel, glowing gradient fills, spring counters, staggered reveal, replay button |
| glass-ai-compose | published | Glass AI Chat — model switcher (Claude/ChatGPT/Perplexity/Gemini), image upload, web search toggle |
| elastic-string | deleted | Removed by user request |
| andromeda-button | published | First DS promotion — re-export wrapper around Andromeda design system Button, full showcase layout (Variants/Sizes/With icon/Disabled), boundary retyping pattern |
| ai-job-cards | published | Three swipeable card stacks (Anthropic/Perplexity/Google), brand logos, bookmark toggle, spring animations — mobile fix applied (h-full→min-h-screen, alignItems:stretch on narrow) |
| peel-corner-reveal | published | "Peel to Scan" — 2D SVG peel, tap-toggle with hover-peek, Wi-Fi QR reveal (qrcode.react), inverted light/dark card, pulsing Wi-Fi arcs, +31° QR rotation, inlined useTheme for copy-paste readiness. Registry + screenshot live. |
| jar-of-emotions | published | SVG glass jar + spring-hinged lid (Framer Motion), 6 reaction emojis (🔥👌😄😭🧑‍🍳🐐), matter.js physics floor + walls, ballistic aim to clicked button, 30-tap "Full. Please Stop." banner, dualTheme. |
| good-vibes | published | Interactive typography — cursor-proximity weight animation (100→700) + 2x scale + letter-spacing, burnt sienna on charcoal/light gray, Science Gothic, three platform prompts, ImageKit live, JSON check PASS, commit ef88c43 pushed |
| playful | published | Interactive typography STAY / WEIRD (two rows) — cursor-proximity 600px, rotation (±75°), alternating jump/drop (±100px, even up / odd down), scale (1→1.4), olive/sand-950 inverted palette, 2px sticker drop-shadow, fluid clamp(2rem, 20vw, 12rem), three platform prompts, ImageKit live, JSON check PASS |
| flip-grid | deleted | Removed by user request |
| liquid-button | deleted | Removed by user request |
| scatter-field | deleted | Removed by user request |
| cube-carousel | published | 3D photo cube (W:H:D = 16:9:9), 6 Unsplash faces with inset vignette, drag-only via Framer onPan, release coasts with soft spring (no snap, no buttons), raw hex chrome, dualTheme — inspired-by rewrite of @fancy/box-carousel (renamed from pivot-cube). JSON check PASS, ImageKit screenshot live, PR #17 merged, aicanvas.me/r/cube-carousel.json returns 200 |
| tilted-coverflow | published | 3D coverflow card carousel of 7 graffiti photos, fan with size-graded scaling (1.0/0.88/0.76/0.64), rotateY 14 deg per step, GAP_PX=30, circular wrap (always 3+1+3), three-layer DOM (positioning button / breathing card / inner image+gradient+caption), entrance stagger (outer-first focus-last bouncy spring), ambient breathing per-card y +/- 12 rotate +/- 1.5 7-10s loop, word-by-word caption fade on focus, drag/click/arrow-key interactions, mobile collapse to focus +/- 1, dualTheme, Motion + Tailwind stack + About paragraph + use-case chips for v2 page layout. Commit 09b26a0, pushed to ideation then merged to main by user; aicanvas.me/r/tilted-coverflow.json returns 200 |
| meet-the-crew | published | **Folder is `components-workspace/avatar-picker/` (internal name); public slug is `meet-the-crew`.** This is why "avatar-picker" looked deleted/missing: it ships under a different name. Swipeable portrait card stack — four cards fan out with soft rotations, drag to browse, tap to choose. For team / About-Us sections, character selectors, or avatar pickers. Live: /components/meet-the-crew (200), aicanvas.me/r/meet-the-crew.json (200), present in the registry.json index. Verified + added to tracker 2026-06-09. |
| curious-ai | published | Was missing from this tracker; fully shipped + live (200). Morphing 3D AI orb (Three.js) with cyan/magenta rim lights, iridescent pink speckles on a wrinkled surface, glowing pink robot-eye pills. Deps: three, framer-motion. Added 2026-06-09. |
| danger-stripes | published | Was missing from this tracker; fully shipped + live (200). Three crossing caution-tape stripes on orange; hover to shake, click to intensify. Deps: framer-motion. Added 2026-06-09. |
| emoji-burst | published | Was missing from this tracker; fully shipped + live (200). Button that explodes emojis outward in all directions; five emoji sets cycle on each click. Deps: framer-motion. Added 2026-06-09. |
| orbit | published | Was missing from this tracker; fully shipped + live (200). Kinetic text arranged in a circle; hover slows the spin and letters push outward. Deps: framer-motion. Added 2026-06-09. |
| signature-pad | published | Was missing from this tracker; fully shipped + live (200). Signature pad widget; pill button morphs into a canvas to draw with mouse or touch. Deps: framer-motion, @phosphor-icons/react. Added 2026-06-09. |
| upload-progress | published | Was missing from this tracker; fully shipped + live (200). Collapsible file-upload widget with shimmer progress bar (indigo uploading, amber paused); expand for per-file rows with live progress. Deps: framer-motion, @phosphor-icons/react. Added 2026-06-09. |
